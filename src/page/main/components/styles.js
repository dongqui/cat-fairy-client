import styled from 'styled-components';

const SidebarContainer = styled.aside`
  position: absolute;
  width: 80px;
  height: 80%;
  right: 0;
  top: 50px;
`;

const SidebarList = styled.ul`

`;

const SidebarItem = styled.li`

`;


const CatFeet = styled.img`
  width: 100%;
  background-color: transparent;
`

export default {
  SidebarContainer,
  CatFeet
}
