import styled from 'styled-components';

const SidebarContainer = styled.aside`
  background-color: red;
  position: fixed;
  width: 80px;
  height: 80%;
  right: 0;
  top: 50px;
`;

export default {
  SidebarContainer,
}
